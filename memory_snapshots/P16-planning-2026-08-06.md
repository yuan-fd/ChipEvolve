# P16 规划长期记忆快照

date: 2026-08-06
planning_base: `55c0bde67ecbd9332a7f44d0d5c11ac2a119e1c8`
status: planning_complete_waiting_for_execution_approval

用户确认的方向：公开论文/项目提供 knowledge 与 benchmark，后续真实运行持续积累；DPLEvolve 只做低成本 smoke；开放平台后用户输入自己的 API key 和模型；RL 先给建议，由用户选择，高置信且有明确授权时才有限自动；IC Craft 增加 OpenROAD 后端，不要求商业工具才能使用平台。

P16 定义为“开放知识、BYOK 与人控自演化 v1”，包含：

- P16-A：有 license/commit/hash 的公开 KnowledgeSource、DocumentClaim、BenchmarkDefinition 和 CorpusSnapshot；
- P16-B：Runtime 只读持续 Collector、quarantine/verify/admit、幂等、tenant 私有和 opt-in 共享；
- P16-C：rules、local Codex、OpenAI-compatible BYOK；key 默认只在内存存在 8 小时，不进 SQLite/日志/artifact；
- P16-D：T0 shadow、T1 用户建议、默认关闭的 T2 limited auto；置信度来自上下文、覆盖、校准、约束和证据，不来自 LLM 自评；
- P16-E：backend-neutral FlowPlan，分别翻译到现有 ORFS Runtime 或 ImplCraft 商业脚本生成；
- P16-F：低成本集成验收。

推荐预算：最多 8 个新增 ORFS run、并发 2；DPLEvolve full-flow 0；首批最多 20 来源、下载 5 GiB；真实 BYOK 手工调用最多 5 次且由用户承担。当前 10 条 observation 不满足 T2 是正确结果，不得降低门槛。

DPLEvolve smoke 额外事实：33 knowledge records 通过；Teacher/Student/Review prompt 生成成功，0 warning；`detailed_placement_evolve` 等四个 Tcl command 存在；静态库含 `Dpl_evolve_Init` 与 `runStudentAlgorithm`；没有 Codex/EDA 调用，共享 ORFS 未变化。证据见 `docs/evidence/P15_DPLEVOLVE_SMOKE.md`。

恢复时先读 `tasks/phase-16.md`。规划完成不等于获准实现，下一步必须由用户明确批准推荐审批项。
