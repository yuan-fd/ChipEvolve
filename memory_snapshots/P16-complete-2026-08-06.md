# P16 完成长程记忆快照

date: 2026-08-06
status: completed_and_accepted
planning_commit: `3bc9829`

P16 已实现四项用户要求，并补齐原计划中的持续学习与平台验收：

1. `knowledge/public-corpus.lock.json` 固定 10 个公开来源、7 个 benchmark definition。来源有 URL、版本、许可判断、hash basis 与 SHA-256；许可不清的大文件只登记 metadata。`scripts/import_public_knowledge.py` 可离线重放快照。外部知识永远不是本地 observed。
2. `LearningCollector` 只读消费终态 Runtime evidence，执行 quarantine/verify/admit/reject；幂等键含 tenant/project/run/attempt/context/parser。tenant/project 默认私有，进入共享样本必须 opt-in。
3. 新增 OpenAI-compatible BYOK。非秘密 ProviderProfile 可持久化，key 只在 `InMemorySecretBroker` 保存 8 小时，绑定 owner/session，可撤销。受控 egress 限制 HTTPS、管理员 host allowlist、地址、重定向、超时、大小和调用预算。未调用真实付费 API。
4. BO/GP/RL 输出可转成 T1 PolicyRecommendation，用户接受/修改/拒绝均独立留证。ConfidenceBreakdown 只使用上下文、覆盖、held-out 校准、OOD、安全与 evidence；不使用 LLM 自报。真实 P14 只有 10 条 observation，T2 正确为 not_eligible。
5. `BackendNeutralFlowPlan` 可映射到 `openroad-orfs` TaskSpec 或 `implcraft-scriptgen`。OpenROAD 分支必须经 Registry/Workflow Runtime；商业 signoff capability fail closed。

真实 Craft 验收：

- 首 run `d3b5b90540104414946f3a797b91398b` 因 top `mux2` 与 RTL 声明 `mux_2to1` 不一致，在 synth 失败，证据保留。
- 第二 run `6111c2de98a94b28b7717d2f13cd96a3` 使用 ORFS 官方 gcd，synth/floorplan/place/CTS/route/finish 全成功。
- GDS 631,606 bytes，SHA `2d84c09a04902177def110104753df597b5b43b91082d8d434d65490a4dc6690`；DRC=0，setup WNS=7.4585 ns，area=604.352 um²，wirelength=3799 um。
- Runtime DB SHA `90239d3e...`，登记 18 artifacts、7 metrics、12 tool-stage events，并生成 KLayout 2D view。
- 原始证据目录 `artifacts/p16-craft-real-gcd/`；机器结论 `docs/evidence/P16_OPEN_PLATFORM_ACCEPTANCE.json`。

验收：167 passed；50 个固定知识检索/错配 case；fake Provider 的 success/401/429/timeout/invalid/oversize/secret-echo/cancel/owner/revoke；canary 在持久化面 0 命中；Node/JSON/diff check 通过。

明确未完成/不声称：没有下载大 benchmark 或付费墙论文正文；没有成熟在线 RL；没有开放 T2；OpenROAD 不等于商业 signoff；ImplCraft 未执行商业工具；BYOK key 不跨服务重启保存；当前 Web 的 local-user 不是生产认证。

下一阶段 P17 建议先解决生产认证/ACL/HTTPS/secret manager，再导入用户尚未提供的四篇论文和代码级优化 Agent 源码，扩充真实数据、held-out 校准、OOD 与 Study 配额。恢复时先读 `docs/evidence/P16_OPEN_PLATFORM_ACCEPTANCE.md`、`docs/P16_OPEN_PLATFORM.md` 和 `docs/progress/NEXT_ACTION.md`。
