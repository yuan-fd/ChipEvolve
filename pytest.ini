[pytest]
testpaths =
    guardrails
    contracts/tests
    tests
    apps
pythonpath =
    contracts/src
    core/runtime/src
    core/registry/src
    gateway/src
    plugins/orfs-evaluator
    core/evaluator/src
    core/provenance/src
    core/identity/src
    core/client/src
    guardrails
addopts = -ra --strict-markers
markers =
    real_eda: touches a real EDA toolchain; not run in the default suite
